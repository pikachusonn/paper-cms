type CourtStaff {
  id: ID!
  court: Court!

  name: String!
  phone: String
  email: String
  isDeleted: Boolean!
  responsibleDocs: [Document]
}

extend type Query {
  courtStaffs: [CourtStaff!]!
  courtStaff(id: ID!): CourtStaff
}

